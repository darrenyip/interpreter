#include <stdlib.h>
#include <stdio.h>
#include <stdbool.h>
#include <string.h>


void readFileByLineAndExecute(char* path);
void runPrompt();
void gotoFunc(char label[]);
char* appendCh(char *cArr,const char c);
